# This is a template showing the correct REQUIRED structure.
name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

# The 'jobs' section is mandatory and acts as the container for all execution blocks.
jobs:
  # The 'build-and-push' job is mandatory.
  build-and-push:
    # 'runs-on' must be nested inside a job.
    runs-on: ubuntu-latest

    # 'steps' must be nested inside a job.
    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Use the setup-node action if you are building Node.js code
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'

      # Step 3: Run your build, Docker build, and Docker push commands here.
      - name: Run Docker/Build Steps
        run: |
          echo "Your build commands go here."
          # docker build -t ghcr.io/youruser/repo:latest .
          # docker push ghcr.io/youruser/repo:latest